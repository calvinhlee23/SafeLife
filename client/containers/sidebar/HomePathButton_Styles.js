import {StyleSheet} from 'aphrodite';

const styles = StyleSheet.create({
  HomePathButton: {
    height: "23px",
    color: "#404040",
    fontSize: "13px",
    lineHeight: "16px",
    cursor: "pointer",
  },
});

export default styles;
